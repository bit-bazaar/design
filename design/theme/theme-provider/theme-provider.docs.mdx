---
description: A theme provider that applies a theme to MUI components
---

Wrap MUI components in a `ThemeProvider` to apply a theme to them.

```js
() => {
  return (
    <ThemeProvider>
      <Button>I'm a themed button!</Button>
    </ThemeProvider>
  );
};
```
