---
description: 'A basic-spa app.'
labels: ['app', 'react']
---

A React application for basic-spa. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use basic-spa 
```

To run the app locally, run the following:

```
bit run basic-spa 
```
